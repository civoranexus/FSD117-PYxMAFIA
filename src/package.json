{
  "name": "fsd117-pyxmafia",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm run install:all && npm run build:frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "start:prod": "npm run build:frontend && npm run start --prefix backend",
    "dev": "concurrently -n backend,frontend -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
